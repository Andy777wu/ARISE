**AI 开发通用基础规则**

#### 1. 通用原则
- 优先采用**简单直接**的解决方案，避免过度设计。
- 非必要**不引入**新的依赖或抽象层。
- 除任务明确要求外，**不得**进行无关的重构或代码风格调整。
- **保持现有行为不变**，除非用户明确要求修改。
- 优先复用现有的公共组件、工具函数和基础库。

#### 2. 编码前准备（需求分析与方案设计）
在修改任何代码之前，必须按顺序完成以下评估：
1. **全面阅读**：通读待修改模块及相关上下游文件。
2. **理解架构**：掌握当前系统的分层设计、数据流向和核心设计模式。
3. **影响面分析**：识别修改可能带来的依赖变更和潜在副作用。
4. **最小化方案**：提出**改动范围最小**、**风险可控**的实施方案，并获得确认后再动手。

#### 3. 前端规范
- **技术栈**：React + Vite。
- **编程语言**：默认使用 JavaScript；若项目体量较大或类型安全要求高，则优先采用 TypeScript。
- **组件设计**：组件必须遵循**单一职责**原则，功能边界清晰。
- **接口请求**：所有后端 API 调用必须通过**统一的 API 层**（如 `api/` 或 `services/`）进行，禁止在 UI 组件内直接发起 `fetch` 或 `axios` 请求。
- **逻辑分离**：**严禁**将业务逻辑（如数据处理、状态计算）直接写在 UI 组件内部，应抽离至 Hooks、工具函数或状态管理库中。

#### 4. 后端规范
- **技术栈**：Python + FastAPI。
- **分层架构**：严格遵循 **Router（路由层） / Service（业务逻辑层） / Repository（数据访问层）** 职责分离。
- **数据校验**：所有外部输入（Request Body、Query、Path 参数）必须使用 **Pydantic** 模型进行声明与校验。
- **逻辑归属**：核心业务规则和流程编排放在 **Service 层**；数据库的 CRUD 操作封装在 **Repository 层**。

#### 5. API 接口规范
- **契约一致性**：保持 API 接口定义（路径、方法、参数）的稳定与统一。
- **破坏性变更管控**：未经明确审批，**不得**修改现有接口的响应结构（字段名、类型、状态码）。
- **错误可观测性**：错误响应必须具有**标准化格式**（如统一的 `code/message/data` 结构），并包含便于日志追踪的 `request_id` 或 `trace_id`。

#### 6. 代码交付与验证
在完成功能开发后，必须执行以下自测流程：
- **静态检查**：运行 Lint 工具（如 ESLint、Pylint）和代码格式化检查。
- **自动化测试**：运行相关的单元测试和集成测试，确保覆盖率不下降。
- **接口联调**：验证 API 请求/响应逻辑是否符合预期（建议使用 Postman 或 Swagger 文档验证）。
- **前端交互**：验证前端页面渲染、用户操作流和数据更新是否正常。

#### 7. 变更边界控制
**仅修改**完成当前任务所必需的文件。
**严格禁止**以下越界行为：
- 重写或重构与本次需求无关的模块。
- 无正当理由地重命名文件、变量或函数。
- 引入新的架构模式（如引入新的状态管理库、新的设计模式）来“顺带优化”。
- 添加当前需求未明确提及的“锦上添花”式功能或预留接口。
